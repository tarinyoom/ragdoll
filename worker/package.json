{
	"name": "worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --outfile=../dist/worker/index.js --platform=browser --target=es2021 --format=esm",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"esbuild": "^0.25.5",
		"typescript": "^5.5.2",
		"wrangler": "^4.20.3"
	}
}
